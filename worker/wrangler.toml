# 通知用 Durable Object 宿主 Worker（店员端站内实时响铃第二层）
# 步骤 B：重新绑定并新建 OrderNotifier 类（clean state）
name = "ai-cc-prod-notifier"
main = "src/index.ts"
compatibility_date = "2025-08-01"

[[durable_objects.bindings]]
name = "Notifier"
class_name = "OrderNotifier"

[[migrations]]
tag = "v5"
new_sqlite_classes = ["OrderNotifier"]
